import React from 'react';

export default function Catalog(props) {
    const { style } = props;
    // console.log(style);
    return (
        <div style={style}>
            Catalog
        </div>
    )
}